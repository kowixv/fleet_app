# Durum & Kaldığım Yer — 2026-07-12

> **Canlı ile kod artık aynı hizada.** 3 Temmuz'da yazılan 24 denetim düzeltmesinin
> migration'ları canlı Supabase'e uygulandı ve kod GitHub'a push edilip Vercel'e deploy oldu.
> `DURUM-2026-07-03.md`'deki "yarın ilk iş" listesi **kapandı**.

---

## ✅ Bugün yapılanlar

### 1. Klasör temizliği
`fleet-app-yukle/` (2 Temmuz öncesi eski kopya) ve `fleet-app-yukle.zip` silindi.
Tek kaynak: **`fleet-app`**. `fleet-tablet` ayrı proje olarak duruyor (Expo/Android).

### 2. Migration'lar canlıya uygulandı
`supabase/migrations/20260703100000/100001/100002` sırayla, her biri kendi transaction'ında.

**Yolda çıkan tek sorun:** `pgcrypto` Supabase'de `public` değil **`extensions`** şemasında
duruyor; migration'daki çıplak `digest()` çağrısı bu yüzden hata verecekti. Hem
`20260703100001_tablet_token_hash.sql`'e hem `schema.sql`'e
`set search_path = public, extensions;` eklendi — böylece pgcrypto'nun `public`'te olduğu
düz bir Postgres'te de çalışır.

**Doğrulandı (11/11):** `tablet_tokens.token` kolonu düştü · 6 token'in tamamı geçerli
SHA-256 hash · eski `_rw` politikalarının hepsi kalktı · 20 tabloda tam
`select/insert/update/delete` seti · `profiles` yalnızca SELECT (rol yükseltme deliği kapandı) ·
`organizations`'ta DELETE yok (tenant silme deliği kapandı) · `current_user_role()` +
`is_org_writer()` oluştu · 3 yeni index yerinde.

### 3. Push + deploy
Commit `88065c5` → `github.com/kowixv/fleet_app` → Vercel otomatik deploy.
`npm test` 56/56, `npm run build` temiz. Canlıda `/login` 200 ve **CSP header'ı dönüyor**
(yeni kodun deploy olduğunun kanıtı).

> Not: bu makinedeki git kimliği `hiatech`, repo ise `kowixv` hesabında. Push,
> `.env.local`'daki `GITHUB_TOKEN` (kowixv'e ait) ile yapıldı. Normal `git push` bu
> makinede 403 verir.

### 4. Teslim paketi
`D:\Projeler\fleet\fleet-teslim-2026-07-12.zip` — `fleet-app` + `fleet-tablet`,
`node_modules`/`.next`/`.git` hariç, `.env.local` dahil (ama `GITHUB_TOKEN` ve
`VERCEL_OIDC_TOKEN` çıkarılmış), kökte `OKU-BENI.md`.

---

## 📋 Sıradaki işler

1. **Uçtan uca tablet testi** (hâlâ bekliyor — 6 token var ama hiçbiri bağlanmamış,
   `last_seen_at` boş): Settings → Token Oluştur → sürücü linkini telefonda gizli sekmede
   aç → Takibi Başlat → `/tracking` haritasında araç + "Son Aktiflik: just now".
   Yük kartı için yük **booked** + araca atanmış olmalı (eski yükleri loads'ta bir kez
   düzenle-kaydet → geocode tetiklenir).
2. (Ops.) Vercel'e `FLEET_TIMEZONE` env'i — koddaki varsayılan `America/Chicago`,
   sadece farklı TZ istenirse gerekli.
3. (Ops.) `GOOGLE_ROUTES_API_KEY` — gerçek yol bazlı ETA; yoksa kuş uçuşu mesafe.
4. Native **fleet-tablet** (Expo/Android) build + cihaza kurulum. `/drive` PWA'sı
   o zamana kadar yeterli.
5. `.env.local`'daki `GITHUB_TOKEN` duruyor — push için gerekli olduğundan bilinçli olarak
   bırakıldı. Dosya `.gitignore`'da ve teslim zip'ine girmiyor. Push yolunu başka bir şekilde
   kurarsan (git credential manager) bu satırı silebilirsin.

## 📁 Hızlı referans
Repo: github.com/kowixv/fleet_app · Canlı: fleet-app-olive.vercel.app ·
Supabase: nyguwwvqqjptietfeeam · Rol bazlı RLS aktif: `viewer` artık yazamaz.
